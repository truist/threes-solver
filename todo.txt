
prior art:
	https://toucharcade.com/community/threads/threes-by-sirvo-llc.218248/page-27
	https://www.reddit.com/r/compsci/comments/33xmtt/comment/cqph6tp/
	http://blog.waltdestler.com/2014/04/threesus.html
	https://youtu.be/pmKtRxw_fsg?t=213

	https://github.com/waltdestler/Threesus?tab=readme-ov-file
	https://stackoverflow.com/questions/22342854/what-is-the-optimal-algorithm-for-the-game-2048
	https://github.com/nneonneo/threes-ai


game algorithm:
	4x4 board with the numbers

	cards are added from a "stack" of 12 cards
		4x1, 4x2, 4x3
		when the stack is empty, a new stack is created and shuffled

	board seeded with 9 cards
		from the first stack
		after some period, one of those 9 will be high, i.e. not from the stack
			and will be in the top-left corner
			seems to be 4 ranks below the highest card you've ever seen
			doesn't seem necessary for the solver to have this feature

	you "shift" the board in one of the cardinal directions
		for e.g. a left (west) shift:
			cards shift to the left if possible, starting with the left-most card:
				if there's an empty space to the left
				if there's a card with the same value, to the left, it will merge (doubling the value)
					and 1+2 will merge (into 3)
			if those rules don't match, the card stays in place
			if a card shifts (including by merging) it opens the space it was in
				so there will only ever be 1 merge per row
			if none of the rows can shift any cards, then the shift isn't allowed

	a new card is always added at the "back" of a shift
		in one of the rows/cols that shifted
			with the row/col chosen at random

	the next card is previewed, before each shift

	there are also bonus cards, independent of the regular stack
		starting when the highest card seen in that game is a 192
			"highest card seen" includes the initial seed high-card, if any
				but we aren't implementing the initial seed high-card
		each draw, there's a 1 in 21 chance of getting a bonus card?
		if the next card will be a bonus card, it will be one of three cards
			minimum 6, maximum "highest card seen" divided by 8
			always a contiguous sequence of 3 values (e.g. 12, 24, 48)
			all three will be previewed
			only one will be chosen



components / stages:

+	simulator
+		generates initial game state
+			what's the assumed "high card" state?
+				none
+		performs state transitions / generates new states
+		be careful to keep the state representation internal, for later optimization
+		controllable seed for random elements
+		unit tests
+		compare it to the real app to see if it really "feels" the same
+			especially the random row/column selection
+			and sequences of 1/2 insertion

+	display
+		https://github.com/crossterm-rs/crossterm?tab=readme-ov-file
+		show the state
+		take user input to play

+	auto-play (basics)
+		given a game state, decide what to do
+		some sort of scoring function (see below)
+			with unit tests

+	optimize the auto-play
+		give each algo a weight
+		alter weights until an optimal scoring algorithm is determined
+		pre-next:
+			fix all the warnings
+			clean up the code
+			figure out if RNG seed is variable, and make it so if not
+			make RNG seed user-providable
+			stabilize randomness
+				and/or clean out the simulator's random seed stuff
+			(get back to performance improvements)
+		<next:>
+			performance improvements
+				look at the charts more
+				try to stabilize noise, or define better stop conditions
+				parallelize?
+					not easy with rng
x					maybe pre-generate a set of GAMES seeds
x						then always use the associated generated seed for each play()
x							that would reduce noise!
x								but still test many cases
x									and would be different each time the tool ran
+				profiling
x				caching
x					of what?
x						what's the expensive thing?
+			figure out why some runs only get a max value of 12!?!
+			clean up dead code

+	continue rounding out what I have
+		some way to "play" via the scoring function, observing what decisions it makes
+			and why it makes them
+				maybe print out a sorted list of the score parts
+		add tests to confirm that the weights really are being taken into account

+	tidy up:
+		clean up _last_move_dir
+		split algo.rs into multiple files
+		make MovesScaled print better under 'simulate'
+		add tests for the files that don't have any
+		add a way to easily dump weights (from the optimizer) and load them for simulation runs
+		add a way to run a full batch of simulations and report the aggregate results (as happens after an optimize run)

	next:
+		performance tuning
+			convert all the base algo scores to f64 to avoid results that "step"?
x			the filters can probably be performance-optimized by implementing them as bonuses in the regular algo
x				this whole idea has some issues:
x					it prevents future tuning of the weights for each scaler/filter
x					it hard-codes the relative weights of each scaler at 1
x					it makes it impossible to have scalers that only apply to other scalers, not to the base
x			cache/memoize board states?
+		clean up:
+			maybe some renames / clarity improvements
+			refactor merges
+			make WeightedAlgo impl Algo?
+			drop the --weights arg
+		work on the cross-cutting algos
+		maybe the 1/2 filters need to "boost" rather than "filter"?
+			so cmaes can treat their behavior independently from the "base" behavior?
+				that's how the Moves and Empties scalers work
+		split up wrapper.rs
+		(finish tuning the weights after all the changes)

+	auto-play (advanced)
+		take into account all possible next-card insertion possibilities
+			and score based on the combination of all of them
+		performance profiling!
+		take into account all three bonus card possibilities
+			and score based on the combination of all of them
-		reward "risky" moves somehow? (e.g. to rescue trapped low-value cards, on the edge)
-			should happen automatically as a consequence of considering all possibilities
				or maybe only with lookahead?

+	more algos!
-		add more algos and see if the worst-case gets better
-			(and the best case)
+		consider whether some algos should be inherently scaled
+			e.g. if they always get "squashed" by the solver

+	we interrupt this program to focus on cross-machine differences:
+		get more stuff into run output:
+			version (git hash + modification indicator)
+			CPU details
+			Rust version
+			Cargo.lock
+			compiled architecture
+		sort out why there are such big differences across machines
+			simulate individual games and diff them
-			treat small float differences as equal, and use a secondary key as a tie breaker
-			try the same settings across machines and let them run to completion, and see if they get effectively-the-same results
+		make simulate --batch multi-threaded
+		get run output into git
+			threads, lookahead settings, seed, version (git hash), CPU
+		test behavior with various settings:
+			--single-insertion-point
+			--lookahead-depth 3
+			--lookahead-depth 4
+		make 'simulate' colorization better
		improve output of 'simulate'
+			sorting
+			line up details in columns
+			summarize the actual algo results
				and re-normalize?
					they aren't quite "balanced", in practice, which throws off the meaning of the relative weights
					maybe do a summary across 5000 runs
			try to call out "these algos are the ones that really triggered the choice of direction"
				i.e. by having high variance across the options
					especially for the one chosen
			do some analysis of "algo performance" across a run
				"high variance at the beginning"
				"N% of the time it was a key factor in the choice"
					"mostly at the end"

	auto-play look-ahead
+		look at some games that get to 1536 and see why they fail
+			that will give clues toward new algos or scaling factors that help get it to the next level
+				(I think it just needs lookahead)
+		look-ahead to possible future states (without knowing the next card)
+			how to score it?
+				try all 4 directions, from the new state
+				score them
+				do we ignore the tier-1 score, and only count the tier-2 scores?
+						yes
+				average them? best case? worst case?
+			look two steps into the unknown?
+			look-ahead distance, as a variable?
		try variations of lookahead:
			(use --rough)
			we need to penalize hitting dead ends, somehow
			do something other than "average all the possible downstream moves"
				look at individual simulated games
					keep track of seeds, for comparison
				what if only 1 downstream move was possible - should that count against it?
					so maybe sum them?
		try to predict the likely next card, based on recent history?
		have a distinct set of weights for the lookahead scoring?
		test whether lookahead 3, when optimizing, gives better weights than lookahead 2, when optimizing, when running with lookahead 2.
		do a giant pass to figure out which algo variants are most valuable, again


	create specific complex real-world board states with tricky next moves, and see how the algos perform

	optimize performance (as needed)
		test on different machines and see if the threading is efficient
+		some way of logging/graphing progress?
		maybe some way to serialize state, if it really takes forever to run and might be interrupted
+		parallelize
+		profile
		tune look-ahead
		maybe pack the data structures tighter for better cache optimization
		maybe pre-calculate transitions and look them up

	turn on cmaes restarts and do some serious runs
		do we need to scale all the algo scores to the same absolute range, to tell which algos are most valuable?

	clean up all the TODOs in the source
	make weights.toml more human-readable

	publish it
		comment the code more
		with a readme
		with a license

board state score ideas:
	see comments in algo.rs


meta-score ideas:
	max(moves)
	max(threes score)
	avg(high card seen)
		should multiple instances of a high card count for more?
	min(low-scoring games)
	simpler rule sets are better
	less lookahead is better
	highest percent games with highest value visible
		or maybe sum the percent with the top two values
		or maybe just the average max value seen
	(time to first board clear of 1/2)
		(in some specific tricky test case)

